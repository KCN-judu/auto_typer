Desktop packaging resources live here.

- `icon.png`: source artwork used to generate platform icons.
- `icon.icns`: macOS app icon consumed by `electron-builder`.
- `icon.ico`: Windows app icon consumed by `electron-builder`.

If the product branding changes, regenerate both platform icons from the same source artwork before cutting a release.
